# SMC 阶跃跟踪基础配置（经典 sign 切换函数）

y0: [0.0, 0.0]

dt: 0.01
t_end: 10.0
method: RK45

plant_type: MassSpringDamper
plant_params:
  m: 1.0
  c: 0.5
  k: 2.0

controller_type: SMC
controller_params:
  m: 1.0
  c: 0.5
  k: 2.0
  lambda_: 5.0
  eta: 1.0
  # smoothing: null     # 经典 sign（默认）
  # phi: 0.1            # 仅 smoothing 非 null 时有效
  # u_min: -50          # 可选限幅
  # u_max: 50

reference_type: Step
reference_params:
  value: 1.0
  t_step: 0.0

label: "SMC(λ=5, η=1, sign)"
